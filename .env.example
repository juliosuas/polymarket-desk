# Polymarket Desk environment variables
#
# Market data uses public Polymarket endpoints and does not need credentials.
# Watchlist persistence needs Vercel KV / Upstash Redis REST credentials.

KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
